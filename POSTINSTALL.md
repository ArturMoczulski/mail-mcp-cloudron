The MCP endpoint is available at `https://{{CLOUDRON_APP_DOMAIN}}/mcp`.

Configure the client with the static bearer token stored in the app environment. Sending remains disabled until an explicit recipient allowlist is configured.
